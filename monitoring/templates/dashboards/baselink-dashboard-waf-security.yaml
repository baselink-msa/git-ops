apiVersion: v1
kind: ConfigMap
metadata:
  name: baselink-dashboard-waf-security
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  05-waf-security.json: |-
    {
      "title": "BaseLink - WAF Security",
      "uid": "baselink-waf-security",
      "tags": ["baselink", "waf", "security"],
      "timezone": "browser",
      "schemaVersion": 39,
      "version": 2,
      "refresh": "15s",
      "time": { "from": "now-30m", "to": "now" },
      "templating": {
        "list": [
          {
            "name": "datasource",
            "type": "datasource",
            "query": "prometheus",
            "current": {}
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "title": "[WAF] Rule Match Count - Last 5m",
          "type": "timeseries",
          "gridPos": { "h": 10, "w": 24, "x": 0, "y": 0 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "description": "Shows recent WAF rule matches as a rolling 5-minute count. Use this first during load/security tests to confirm which rule matched immediately.",
          "targets": [
            {
              "expr": "sum by (dimension_WebACL, dimension_Rule) (sum_over_time(aws_wafv2_blocked_requests_sum{dimension_Rule!=\"ALL\",dimension_Rule!=\"\"}[5m]))",
              "legendFormat": "{{ "{{" }} dimension_WebACL {{ "}}" }} / {{ "{{" }} dimension_Rule {{ "}}" }} blocked",
              "refId": "A"
            },
            {
              "expr": "sum by (dimension_WebACL, dimension_Rule) (sum_over_time(aws_wafv2_counted_requests_sum{dimension_Rule!=\"ALL\",dimension_Rule!=\"\"}[5m]))",
              "legendFormat": "{{ "{{" }} dimension_WebACL {{ "}}" }} / {{ "{{" }} dimension_Rule {{ "}}" }} counted",
              "refId": "B"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "short",
              "custom": {
                "drawStyle": "bars",
                "lineInterpolation": "stepAfter",
                "fillOpacity": 35,
                "showPoints": "never",
                "stacking": { "mode": "none", "group": "A" }
              }
            },
            "overrides": []
          },
          "options": {
            "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["lastNotNull", "max"] },
            "tooltip": { "mode": "multi", "sort": "desc" }
          }
        },
        {
          "id": 2,
          "title": "[WAF] Blocked / Counted Requests - Last 5m",
          "type": "timeseries",
          "gridPos": { "h": 8, "w": 24, "x": 0, "y": 10 },
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "description": "Simple rolling 5-minute request count by WebACL. This focuses on blocked and counted requests only.",
          "targets": [
            {
              "expr": "sum by (dimension_WebACL) (sum_over_time(aws_wafv2_blocked_requests_sum{dimension_Rule=\"ALL\"}[5m]))",
              "legendFormat": "{{ "{{" }} dimension_WebACL {{ "}}" }} blocked",
              "refId": "A"
            },
            {
              "expr": "sum by (dimension_WebACL) (sum_over_time(aws_wafv2_counted_requests_sum{dimension_Rule=\"ALL\"}[5m]))",
              "legendFormat": "{{ "{{" }} dimension_WebACL {{ "}}" }} counted",
              "refId": "B"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "short",
              "custom": {
                "drawStyle": "bars",
                "lineInterpolation": "stepAfter",
                "fillOpacity": 30,
                "showPoints": "never",
                "stacking": { "mode": "none", "group": "A" }
              }
            },
            "overrides": []
          },
          "options": {
            "legend": { "displayMode": "table", "placement": "bottom", "calcs": ["lastNotNull", "max"] },
            "tooltip": { "mode": "multi", "sort": "desc" }
          }
        }
      ]
    }
